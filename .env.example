# Copy to .env for local development. Never commit the real .env.

# --- Database: pick ONE path ---
# Local/dev (what this session's Homebrew Postgres 17 + pgvector uses):
DATABASE_URL=postgresql+psycopg://localhost:5432/research_copilot_dev

# Lakebase (production) — leave DATABASE_URL unset to use this path instead.
# This workspace's real values (confirmed live 2026-08-15) — instance name still needed,
# see project memory:
# DATABRICKS_PROFILE=dbc-7b106152-caf3           # requires `databricks auth login --profile dbc-7b106152-caf3` first
# LAKEBASE_INSTANCE_NAME=                        # not yet known — CLI session was invalid when we checked
# LAKEBASE_HOST=ep-proud-sound-d1d47y55.database.us-west-2.cloud.databricks.com
# LAKEBASE_DB_NAME=databricks_postgres
# LAKEBASE_DB_USER=nimbalkarvicky9@gmail.com
# LAKEBASE_PORT=5432
#
# Alternative auth to DATABRICKS_PROFILE — a service principal (better for a deployed app):
# DATABRICKS_HOST=
# DATABRICKS_CLIENT_ID=
# DATABRICKS_CLIENT_SECRET=
#
# One-off manual check only (expires ~1hr, no refresh) — never commit a real value here:
# LAKEBASE_STATIC_TOKEN=

# --- Databricks Foundation Model APIs (agent + embeddings) ---
# Prefer DATABRICKS_PROFILE (OAuth via `databricks auth login`) over DATABRICKS_FM_TOKEN —
# a static PAT may be disabled by org policy; the OAuth path doesn't need one.
# DATABRICKS_FM_BASE_URL=https://<workspace-host>/serving-endpoints
# DATABRICKS_FM_TOKEN=
# LLM_MODEL=databricks-claude-haiku-4-5
# EMBEDDING_MODEL=databricks-qwen3-embedding-0-6b

# --- OpenAlex (optional — works without a key for casual use) ---
# OPENALEX_API_KEY=
# OPENALEX_MAILTO=
